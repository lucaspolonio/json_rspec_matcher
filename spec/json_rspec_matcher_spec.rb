require 'spec_helper'

describe JsonRspecMatcher do
  it 'has a version number' do
    expect(JsonRspecMatcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
